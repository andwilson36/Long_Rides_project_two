function setValue(pickup, destination) {
    let pickupInput = document.getElementById('#start');
    pickupInput.value = pickup;
    let destinationInput = document.getElementById('#end');
    destinationInput.value = destination;
  }